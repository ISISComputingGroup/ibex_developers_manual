todo: because we are doing everything in event mode there is no setting to be set in hardware. filewriter will write everything in event mode, and not histogramming. 
kdae_diagnostics will use tcbs but nothing will set them in the hardware. 
this adr might be pending due to live view shenanigans. 